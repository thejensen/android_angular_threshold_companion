export class Event {
  constructor (public name: string, public location: string, public link: string, public date: string, public description: string, public imageUrl: string, public categoryCause: string, public categoryAction: string, public price: string, public countThreshold: number, public countActual: number, public happening: number) {}
}
